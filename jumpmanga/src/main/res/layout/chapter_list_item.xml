<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="50dp"
    android:layout_margin="@dimen/spacing_medium"
    android:background="@drawable/custom_background_indicator">

    <TextView
        android:id="@+id/name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_marginLeft="@dimen/spacing_medium"
        android:layout_marginRight="90dp"
        android:fontFamily="sans-serif-light"
        android:text="Chap 356"
        android:textColor="@android:color/black"
        android:textSize="18sp" />

    <CheckBox
        android:id="@+id/favorite_box"
        android:layout_width="25dp"
        android:layout_height="25dp"
        android:layout_centerVertical="true"
        android:layout_alignParentRight="true"
        android:layout_marginRight="5dp"
        android:background="@drawable/star_check_box"
        android:button="@null"
        android:focusable="false" />

    <ImageView
        android:id="@+id/status_read"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_centerVertical="true"
        android:layout_marginRight="5dp"
        android:layout_marginLeft="4dp"
        android:layout_toLeftOf="@id/favorite_box"
        android:paddingTop="2dp"
        android:src="@drawable/ic_read"/>

    <ImageView
        android:id="@+id/download"
        android:layout_width="27dp"
        android:layout_height="27dp"
        android:layout_toLeftOf="@id/status_read"
        android:layout_centerVertical="true"
        android:paddingTop="2dp"
        android:src="@drawable/downloaded_indicator"/>

</RelativeLayout>